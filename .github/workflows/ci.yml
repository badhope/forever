name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "20"

      - name: Detect package manager
        id: pm
        run: |
          if [ -f pnpm-lock.yaml ]; then echo "pm=pnpm" >> "$GITHUB_OUTPUT"
          elif [ -f yarn.lock ]; then echo "pm=yarn" >> "$GITHUB_OUTPUT"
          elif [ -f bun.lockb ] || [ -f bun.lock ]; then echo "pm=bun" >> "$GITHUB_OUTPUT"
          else echo "pm=npm" >> "$GITHUB_OUTPUT"; fi

      - name: Install pnpm
        if: steps.pm.outputs.pm == 'pnpm'
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v4.1.0
        with:
          version: 9

      - name: Cache deps
        uses: actions/cache@v5
        with:
          path: |
            node_modules
            ~/.pnpm-store
            ~/.cache
          key: ${{ runner.os }}-${{ steps.pm.outputs.pm }}-${{ hashFiles('**/package-lock.json', '**/pnpm-lock.yaml', '**/yarn.lock', '**/bun.lock', '**/bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.pm.outputs.pm }}-

      - name: Install
        run: |
          case "${{ steps.pm.outputs.pm }}" in
            pnpm) pnpm install --frozen-lockfile ;;
            yarn) yarn install --frozen-lockfile ;;
            bun)  bun install --frozen-lockfile ;;
            npm)  npm ci ;;
          esac

      - name: Lint
        run: |
          if grep -q '"lint"' package.json; then
            case "${{ steps.pm.outputs.pm }}" in
              pnpm) pnpm run lint ;;
              yarn) yarn lint ;;
              bun)  bun run lint ;;
              npm)  npm run lint --
            esac
          fi

      - name: Format check
        run: |
          if grep -q '"format:check"\|"format"' package.json; then
            case "${{ steps.pm.outputs.pm }}" in
              pnpm) pnpm run format:check || pnpm run format -- --check ;;
              yarn) yarn format:check || yarn format --check ;;
              bun)  bun run format:check || bun run format --check ;;
              npm)  npm run format:check -- --if-present || npm run format -- --check
            esac
          fi

  test:
    name: Test / Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "20"

      - name: Detect package manager
        id: pm
        run: |
          if [ -f pnpm-lock.yaml ]; then echo "pm=pnpm" >> "$GITHUB_OUTPUT"
          elif [ -f yarn.lock ]; then echo "pm=yarn" >> "$GITHUB_OUTPUT"
          elif [ -f bun.lockb ] || [ -f bun.lock ]; then echo "pm=bun" >> "$GITHUB_OUTPUT"
          else echo "pm=npm" >> "$GITHUB_OUTPUT"; fi

      - name: Install pnpm
        if: steps.pm.outputs.pm == 'pnpm'
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v4.1.0
        with:
          version: 9

      - name: Cache deps
        uses: actions/cache@v5
        with:
          path: |
            node_modules
            ~/.pnpm-store
            ~/.cache
          key: ${{ runner.os }}-${{ steps.pm.outputs.pm }}-${{ hashFiles('**/package-lock.json', '**/pnpm-lock.yaml', '**/yarn.lock', '**/bun.lock', '**/bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-${{ steps.pm.outputs.pm }}-

      - name: Install
        run: |
          case "${{ steps.pm.outputs.pm }}" in
            pnpm) pnpm install --frozen-lockfile ;;
            yarn) yarn install --frozen-lockfile ;;
            bun)  bun install --frozen-lockfile ;;
            npm)  npm ci ;;
          esac

      - name: Test
        run: |
          if grep -q '"test"' package.json; then
            case "${{ steps.pm.outputs.pm }}" in
              pnpm) pnpm test -- --run ;;
              yarn) yarn test --run ;;
              bun)  bun test ;;
              npm)  npm test -- --watch=false
            esac
          fi

      - name: Build
        run: |
          if grep -q '"build"' package.json; then
            case "${{ steps.pm.outputs.pm }}" in
              pnpm) pnpm run build ;;
              yarn) yarn build ;;
              bun)  bun run build ;;
              npm)  npm run build --
            esac
          fi
